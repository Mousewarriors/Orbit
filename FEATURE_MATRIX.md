# Feature Matrix — honest status

Legend: ✅ done & tested · 🟡 partial / slice · 🧪 logic done, UI/native pending ·
⬜ not started

This file is the source of truth for what actually works. It is intentionally
conservative: a feature is only ✅ when interface + logic + persistence +
permissions + error handling + keyboard + tests are all present.

## Foundation & tooling

| Item | Status | Notes |
| --- | --- | --- |
| Monorepo (npm + cargo workspaces) | ✅ | pnpm also supported |
| Strict TypeScript everywhere | ✅ | `tsconfig.base.json`, all packages clean |
| ESLint + Prettier | ✅ | flat config, 0 warnings |
| Vitest unit tests | ✅ | 351 passing (incl. `@orbit/intent` recogniser/resolver/proposal, the intent-provider glue, the consequential-action confirmation policy, `@orbit/ai-runtime` (Mock + Ollama adapters + validated AI classification), Control Center deep-link encode/decode, the `@orbit/api` SDK suite, the `orbit` CLI end-to-end lifecycle, the AgentOS-controller discoverability checks, desktop route-selection, Root Search discoverability, the application-launch journey, provider-isolation regression, and a Settings render smoke test) |
| Rust unit/integration tests | ✅ | 129 passing across the workspace (orbit-core 46 incl. chat persistence, orbit-extensions 27, orbit-input 15, orbit-desktop 15, orbit-files 10, orbit-search 8, orbit-window-manager 8) + 1 opt-in `#[ignore]` real-launch test. The native HTTP bridge (`http.rs`) adds URL-validation tests run under `cargo test -p orbit-desktop` |
| Centralised branding | ✅ | `packages/branding` |
| Original icon + generator | ✅ | `scripts/generate-icon.mjs` + Tauri icon set |
| CI workflows | 🟡 | `.github/workflows/ci.yml` runs the gate: JS lint + strict typecheck + Vitest + `vite build` (ubuntu), the cross-platform Rust library crates (`cargo test -p orbit-core -p orbit-search -p orbit-window-manager -p orbit-files`, ubuntu), and a Windows job that builds the renderer + `cargo check --workspace` (incl. the Tauri `orbit-desktop`). Not yet run on a live runner here; signing + release jobs remain (DISTRIBUTION.md) |
| Packaging/signing config | 🟡 | `tauri.conf.json` bundle complete (msi/nsis targets, publisher, icons, descriptions, currentUser install); **code-signing not configured** (needs an Authenticode/EV cert — documented in [docs/architecture/DISTRIBUTION.md](docs/architecture/DISTRIBUTION.md)). `tauri build` not run here |
| Single-instance + launch-at-login | 🟡 | `tauri-plugin-single-instance` (registered first; second launch focuses the existing window via `focus_launcher`) + `tauri-plugin-autostart` wired through `get_autostart`/`set_autostart` commands and a real Settings → General toggle (off by default). Compiles (`cargo check`) and is wired; runtime focusing / login-entry creation not GUI-verified here. See DISTRIBUTION.md |
| Diagnostics export | ✅ | Settings → Developer "Copy diagnostics" (version/platform/data paths JSON, no secrets) + "Open data folder" |
| Auto-update | ⬜ | intentionally deferred until a signed update-verification model exists (DISTRIBUTION.md) |

## Native shell (Tauri 2 / Rust)

| Item | Status | Notes |
| --- | --- | --- |
| System tray (open/quit/settings) | ✅ | `lib.rs::build_tray` |
| Global hotkey (Alt+Space) | ✅ | **configurable** at runtime via Settings → General (shortcut recorder); re-registered live and persisted (`general.hotkey`) |
| Settings window (native, standalone) | ✅ | second decorated Tauri window **declared in `tauri.conf.json`** (label `settings`, created/navigated at startup like the launcher; hidden-on-close so it is reused, not destroyed) — this is what fixed the blank window: a *runtime*-created webview strands on `about:blank` in `tauri dev`. `open_settings` (tray / "Open Settings" command / IPC) is show+focus of that window. The renderer selects the Settings UI from the window **label** via `route.ts::selectView` (unit-tested). All seven sections render (General/Appearance/Snippets/Files/Extensions/Privacy/Developer — server-render smoke test + live full-screen capture). Wrapped in a visible React error boundary so a renderer fault can't produce a silent blank window |
| Appearance (theme / opacity / reduced transparency / reduced motion) | ✅ | pure `@orbit/appearance` model (11 tests); applied live to the Settings window, applied to the launcher on next show; addresses low-contrast-on-busy-backgrounds via solid/reduced-transparency mode |
| Launcher window (frameless, on-top, centred, hide-on-blur) | ✅ | `tauri.conf.json` + `lib.rs`; **run end-to-end** — Alt+Space toggle, search, calc, clipboard & snippets views verified live (fixed a startup panic from a malformed `plugins.global-shortcut` config) |
| Text injection / paste (SendInput) | ✅ (Windows) / 🟡 | `orbit-input` crate; powers snippet & clipboard paste into the active app; macOS/Linux return a graceful error |
| SQLite open + migrations | ✅ | `orbit-core`, `user_version` strategy, tested |
| IPC command surface | ✅ | `commands.rs`, all typed `Result`. **Startup race fixed (2026-06-16):** `app.manage(AppState)` now called before `install_bundled()` so the packaged-build webview cannot invoke commands before state is registered; 5 unit tests cover the construction and flag lifecycle; `App.tsx` retries once + shows a friendly error for any residual race. |
| Application enumeration | ✅ (Win/mac/Linux) | Start Menu `.lnk` / `.app` / `.desktop` (fast, synchronous at startup). **Windows also merges UWP / Microsoft Store apps** via `Get-StartApps` in the background (Calculator, Terminal, Notepad, … which have no `.lnk`); `.lnk` wins on a name clash. PowerShell-optional: the `.lnk` baseline always stands |
| Application launch (native) | ✅ (Windows) / 🟡 | `launcher.rs`: classic apps/`.lnk`/files via `ShellExecuteW` **with COM initialised on the calling thread** + explicit "open" verb + `HINSTANCE` return-code checking (a failed launch is a visible error, never silent — this fixed "results appear but don't launch"); UWP/Store apps via `explorer.exe shell:AppsFolder\<AUMID>`. Stale/missing path → clear error. **Verified end-to-end opening real Calculator (UWP) and Notepad++ (`.lnk`) windows.** macOS/Linux fall back to the opener plugin |
| Crash recovery (renderer/ext host) | 🟡 | visible React error boundary contains renderer faults (shows the error + stack instead of a blank window); ext-host has a crash-loop breaker; full auto-restart ⬜ |
| Per-command hotkeys / aliases UI | ⬜ | model exists, no UI |
| Privacy controls (clipboard capture toggle, retention, clear) | ✅ | Settings → Privacy; honoured live by the clipboard monitor (`privacy.clipboard.enabled` / `.retention`) |

## Root Search

| Item | Status | Notes |
| --- | --- | --- |
| Fuzzy matching (exact/prefix/acronym/subsequence/typo) | ✅ | TS + Rust mirror, tested |
| Ranking (usage/recency/pinned/favourite/confidence) | ✅ | tested, with score explanations |
| Cancellable, non-blocking provider orchestration | ✅ | tested (timeout/error isolation). A provider whose `search` **or `canHandle`** throws is isolated and recorded, never global-failing the search; failures are logged (structured) and surfaced as a subtle "some sources unavailable" note in the launcher, while healthy providers' results are unaffected |
| Application provider | ✅ | apps (Win32 + UWP) → `open-path` launch action + "Copy Path"; launch failures surface as a visible error and are not recorded as usage (tested in `launch.test.ts`) |
| Command provider | ✅ | slice (built-ins) |
| Calculator provider | ✅ | slice |
| Snippet provider (root search) | ✅ | placeholder-resolved, gated to ≥2 chars; paste-injects on Enter |
| File provider (root search) | ✅ | queries the local file index, gated to ≥3 chars, runs concurrently under the per-provider timeout; actions: open / reveal / copy path |
| Note provider (root search) | ✅ | title+body FTS, gated ≥2 chars; opens the note in the Notes editor via a push-view action carrying its id |
| Extension command provider (root search) | ✅ | lists commands from enabled, non-crashed extensions; no-view runs in the child, list opens a streaming view |
| **Natural-language intent provider (root search)** | 🟡 | **Deterministic-first NL understanding.** New pure `@orbit/intent` (recogniser + entity resolver + intent→action proposal, 51 tests) drives `intentProvider.ts` (11 tests). Recognises and routes ordinary requests — "Open Calculator", "Continue Orbit", "Show failed sessions", "Open the latest handoff", "Restart Relay", "Scan projects", "Find the document that mentioned Leonard", "Find notes about X", "Open the Orbit folder" — to **existing safe actions** (Control Center deep-links with resolved project / session-status filter, app launch, reveal-folder, file/note search, the narrow Restart-Relay command). **No AI call** for any of these. Known short/entity queries (e.g. "Calculator", "125\*4") fall straight through (recogniser returns null), so exact-match providers and the calculator are unaffected. Unresolved projects yield an honest "no known project — open Projects to scan" fallback. **Honest gaps:** AI-assisted intents (explain/summarise/quick-AI) are recognised but return a "Quick AI not available yet" item with a web-search fallback — never a fake answer (AI runtime is a later slice); agent *preference* ("best coding agent") is captured and shown but not auto-selected (Hermes/Model Gateway own that); consequential intents route to the Launch composer's existing confirm step rather than a dedicated preview screen |
| Clipboard / calendar providers | ⬜ | clipboard has its own view; calendar not started |
| Action Panel (keyboard, secondary actions) | 🟡 | compact menu implemented |
| Diagnostics ("why this ranked") | 🧪 | data produced; no UI |

## Local productivity engines

| Item | Status | Notes |
| --- | --- | --- |
| Calculator (arithmetic, functions, %, units, currency, base) | ✅ | `@orbit/calculator`, 20 tests, no `eval` |
| Clipboard history | ✅ | `arboard` poll monitor → SQLite (dedupe, retention, sensitive-flag heuristic); navigable view with filter, copy-back, **paste-into-active-app (⌘↵)**, delete, pin, sensitive masking. `orbit-core::clipboard` has 6 tests |
| Snippets manager (create/edit/delete/search, paste) | ✅ (Windows) | `orbit-core::snippets` CRUD + FTS (8 tests), validated input (`@orbit/validation`, re-checked natively), Snippets view + root provider; paste resolves placeholders then injects keystrokes. Created/listed verified live |
| Snippets expansion (system-wide keyword) | 🟡 | Pure trigger matcher (`orbit-input`, 10 tests) + pure lifecycle state machine (5 tests) + Win32 low-level-keyboard-hook watcher with a **controllable lifecycle** (start/stop/restart/status, idempotent, clean `UnhookWindowsHookEx` via `WM_QUIT`). IPC: `snippet_watcher_status` / `_set_enabled` / `_restart`. **Opt-in** via `snippets.expansion.enabled` (off by default; toggled live from Settings → Snippets). The live hook path (keystroke into a 3rd-party app) is still not verified end-to-end here (Defender blocks synthetic input). Auto-expansion injects raw content (dynamic placeholders resolve on the manual paste path only) |
| Snippet/Quicklink placeholder engine | ✅ | `@orbit/placeholders`, 13 tests (also used by snippet paste) |
| Quicklinks | ✅ | `orbit-core::quicklinks` CRUD (4 tests), Zod validation with scheme allowlist (`@orbit/validation`, re-checked natively), management view (create/edit/delete/open), root-search provider with `{query}` argument from an alias/title prefix + date/time/uuid resolution; web/mail targets open as URLs, paths via the OS handler. Tags/hotkeys/favourites/custom-icons/import-export are future |
| Window management | ✅ (Windows) / 🟡 | 16 layouts; pure geometry tested (`orbit-window-manager`, 8 tests) + Win32 apply via captured foreground window. macOS/Linux return graceful "Windows only" error |
| Local file search | ✅ (Windows) / 🟡 | **Opt-in, metadata-only.** Pure walker + rules (`orbit-files`, 10 tests), `files` table + FTS5 (`orbit-core::files`, 7 tests), background cancellable/self-superseding rebuild in transactional batches, root-search provider, `reveal_path` (no shell). **Settings → Files** now has: enable, roots, excludes, add-folder defaults (first-run "Index Documents/Desktop/Downloads"), rebuild, **clear index**, live progress, **last-indexed time**, **visible diagnostics** (unreadable folders skipped + unavailable/missing roots), hidden toggle. Robust: missing roots / unavailable drives skipped & reported, permission-denied dirs skipped, no symlink following (loop-safe), search cancellable. **Content indexing** (opt-in, off by default): migration 0007 adds a standalone `files_content_fts`; when enabled, the indexer reads small text/code files (≤256 KiB, allow-listed extensions, bounded to 200k chars, read off-lock) and `file_search` merges content matches after name/path matches (deduped). Stored locally, never uploaded; Settings → Files toggle + privacy copy updated. Tested (`orbit-core` content search/clear, `orbit-desktop` `is_text_ext`). **Remaining:** an incremental fs watcher (still full-rebuild only — `modify/rename → index updates` needs a rebuild today). See [docs/architecture/FILE_SEARCH.md](docs/architecture/FILE_SEARCH.md) |
| Notes | ✅ | `orbit-core::notes` CRUD + FTS5 over title+body, pin/archive (5 tests); Notes view with a searchable list + autosaving editor (debounced + on-blur), ⌘N/⌘⌫, crash-safe (saves on blur/escape); root-search note provider. Markdown is stored/edited as plain text (no rendered preview yet); version snapshots are future |
| Built-in tools (UUID / password / colour / JSON) | ✅ | pure `@orbit/tools` (11 tests): colour conversion (#hex/rgb→hex/rgb/hsl), JSON format/minify, secure password (crypto RNG, guaranteed character classes), UUID; surfaced as instant Root Search results that copy on Enter |
| Emoji & symbols | ⬜ | — |
| System commands | 🟡 | "Open Settings", "Rebuild File Index", "Reindex Applications", "Quit" exist as built-ins; broader system commands ⬜ |

## Extensions, AI, MCP, sync, teams, browser

| Area | Status | Notes |
| --- | --- | --- |
| Extension manifest schema + validation | ✅ | `@orbit/validation` (renderer) + `orbit-extensions::manifest` (host), both tested |
| Extension runtime (host, RPC, broker, storage, crash isolation) | 🟡 | **Foundation built + verified end-to-end via real Node RPC this session** (random-uuid → UUID+copy+storage+toast; uuid-history list; unknown-command error; agentos-status mock list). Isolated child-process host (spawn-per-invocation), versioned schema-validated RPC, permission-brokered effects, namespaced storage (`orbit-core::extstore`), crash-loop breaker, Root Search command provider + list view, two sample extensions. **Not** OS-sandboxed beyond process isolation; uses `node` from PATH. See [docs/architecture/EXTENSION_RUNTIME.md](docs/architecture/EXTENSION_RUNTIME.md) |
| Extension management (Settings) | 🟡 | Settings → Extensions shows, per extension: title, version, **description**, **health** (ready/degraded/unhealthy/disabled), **registered commands**, **requested permissions**, **last error**, a collapsible **Recent logs** block (bounded tail of the child's stderr from the latest run), **Open folder** + **Reload** actions, plus an obvious enable/disable toggle (no hidden menus). **Per-extension reload** re-discovers only that one and resets only its crash breaker (`reload_one`, tested — reloading one extension leaves another's tripped breaker intact). Developer folders (load by path), reload-all, and load-error diagnostics. Enable/disable **persists across restart** (file-backed DB test). Uninstall and live (streaming) log tailing are future |
| Extension SDK (`@orbit/api`) | ✅ (foundation) | Typed, ergonomic TS SDK over protocol v1: `defineExtension`, `List`/`List.Item`/`List.Section`, `Detail`, `ActionPanel`/`Action`, `showToast`, `copyToClipboard`/`openUrl`/`openPath` (the 3 brokered effects), typed `preferences`, namespaced local storage, structured logging to **stderr** (stdout stays the protocol channel), manifest types + `validateManifest`. 20 unit tests. Honest about the one-shot model: `showHUD` maps to a toast (`@experimental`); `pushView`/`popView` throw with guidance rather than silently no-op. `@orbit/extension-sdk` (the original JS helper) still ships |
| Extension CLI (`orbit`) | ✅ (foundation) | `orbit extension create\|dev\|build\|validate\|package\|logs` with 5 working templates (no-view/list/detail/preferences/storage); `create` emits a no-repair-needed extension; `package` writes a real `.zip` (dependency-free, `node:zlib` only). 12-step e2e test (vitest): create → validate → discover → run via the real one-shot protocol → modify → re-run (updated behaviour) → package, across all templates. Builds to `dist`; runs as a real Node bin (verified) |
| Extension store | ⬜ | not started (intentionally deferred) |
| AgentOS controller extension (observational) | 🟡 | `extensions/examples/agentos-controller` (uses `@orbit/extension-sdk`): list agents/sessions/projects, recent activity, pending approvals, agent health; actions open dashboard / agent workspace (open-url), open project folder (open-path), copy status/path. **Adapter interface** with mock (live default, clearly labelled) / local-JSON / HTTP (explicit URL, 4s timeout, graceful errors) — all verified via real Node RPC. **Strictly observational**: no shell/SSH/restart/dispatch/code-exec. Honest gap: protocol v1 doesn't yet forward preference *values* to the child, so json/http activate once a preferences-plumbing slice (storage + UI) lands; mock works now. **Live control path (session 17):** rather than wait on the extension preference-plumbing, AgentOS is now controlled through its **own MCP server** (`server/mcp/server.js`, stdio) via Orbit's native stdio MCP host — one-click **Connect AgentOS** in MCP & Tools registers its 8 real tools, and **AI Chat with tool-use** drives them (search vault / memories / route / handoff / decisions / battle plan). Verified end-to-end against the running AgentOS + Ollama llama3.1. See [docs/architecture/AGENTOS_ADAPTER.md](docs/architecture/AGENTOS_ADAPTER.md) |
| AI runtime foundation (`@orbit/ai-runtime`) | 🟡 | **Provider-agnostic completion contracts + adapters, unit-tested (34 tests); not yet wired to a user surface** (Phase 3 guidance: no AI UI before the runtime is reliable). `AiProvider` (health/listModels/complete/stream, `AbortSignal` cancellation, `local` flag, typed `AiError`); **MockProvider** (deterministic, offline, scriptable, streams); **OllamaProvider** (local models over an injected `fetch` — `/api/tags` + `/api/chat` NDJSON streaming, `format:json`, usage, never auto-pulls). **Validated AI intent classification** (`classify.ts`): the deterministic-first fallback — the model is constrained to emit a single known intent + whitelisted slots, then output is **hard-validated** (unknown intents/fields/agents dropped, strings clamped) so it can only point at an existing safe intent, never widen the action surface. **Deliberately not duplicating the Model Intelligence Gateway** (Auto-mode routing stays server-side). **The OllamaProvider is now live** via the native HTTP bridge (session 12) — `complete`/`stream`/`listModels`/`health` run against a real local Ollama. **The `OpenAiCompatProvider` is also live** (session 16, 4 tests): any OpenAI-compatible `/v1` endpoint (OpenAI, OpenRouter, or a local server like LM Studio/vLLM) over the native fetch — Bearer auth, SSE streaming, `/models`, `response_format: json_object`, local-vs-remote inferred from the host. Its **API key lives in OS secure storage** (Windows Credential Manager via the `keyring` crate; `secret_set/get/delete/has` IPC), never in settings/files/logs; provider construction is centralised in `ai/providerLoad.ts` which pulls the key only for the cloud provider. A native **Anthropic** adapter + **AgentOS Auto** routing remain. **Tool-calling (session 17):** the contract gained `tools`/`toolCalls`; the **Ollama** and **Mock** adapters implement function-calling (Ollama maps tool defs + `tool`/`assistant` tool-call messages and parses `message.tool_calls`; a failed tool request surfaces Ollama's error, e.g. "does not support tools", with guidance). OpenAI-compat + Anthropic tool-calling are follow-ups. See [docs/architecture/AI_RUNTIME.md](docs/architecture/AI_RUNTIME.md) |
| Quick AI (one-shot surface) | 🟡 | **Built end-to-end against the runtime.** `QuickAiView` (reachable via the "Quick AI" command and from any AI-shaped natural-language request, pre-filled): prompt input, optional **Clipboard context chip**, streaming output with a **Stop**, **Copy** + **Paste-into-active-app** (paste gated by the confirmation dialog), a clear **on-device/leaves-device privacy badge**, and bounded **recent prompts**. Pure helpers (`providerConfig` factory + `quickAi` message/recent builders) are unit-tested (12 tests); a **Settings → AI** section selects the provider and saves Ollama endpoint/model. **Status (native AI bridge landed, session 12):** **local Ollama now works for real** — a native Rust HTTP client (`http_request` / `http_stream_open` / `http_stream_cancel`, http(s)-only, validated) bridges the renderer past the CSP, and a native-backed `FetchLike` (`ai/nativeFetch.ts`, event-fed `ReadableStream` so the provider's streaming TextDecoder handles byte boundaries) drives the existing `OllamaProvider` unchanged. Select **Ollama** in Settings → AI (endpoint + model) and Quick AI / AI Commands / mission AI-planning stream real tokens on-device. Mock (clearly synthetic) and None remain. Cloud providers need credential storage (next). Context is treated as untrusted data (prompt-injection guard in the system prompt). |
| AI Commands (starter library) | 🟡 | **Reusable named AI behaviours surfaced in Root Search**, run through Quick AI. Pure `ai/aiCommands.ts` (9-command starter library — Improve Writing, Fix Grammar, Make Shorter/Professional, Summarise, Explain, Translate, Write Commit Message, Create JSON — + `renderAiCommand` `{input}` substitution + a Root Search provider, 6 tests). Selecting one opens Quick AI pre-filled, with the clipboard pre-attached where the command uses it, and **auto-runs**. **Honest scope:** a user-editable command store/editor, custom placeholders ({selection}/{browser}/…), hotkeys and import/export are later; answers are only as real as the configured provider (Mock today). |
| Agent missions / Orbit Agent (`@orbit/mission`) | 🟡 | **Phase 9 foundation — "tell Orbit what to do and it does it", built end-to-end + unit-tested (60 new tests across mission/dispatch/executor/provider).** A goal becomes a **validated plan of whitelisted tool calls**: each step references a Tool Registry id with args validated against the tool schema, so a planner (deterministic *or* model) can only ever assemble Orbit's existing safe capabilities — never a shell string/path/new action (`parseMissionPlan` hard-validates, like `classify.ts`). **Deterministic-first:** `planDeterministically` maps recognised goals ("Continue Orbit with the best agent", "Restart Relay", "Find files about Leonard", "Show failed sessions") to a one-step plan with **no AI call**; AI planning is the fallback and needs a configured provider. **Launching agents is real** — `dispatchAgentViaRelay` drives Relay's audited `listAgents → createLaunchPlan → executeLaunch(confirm:true)` flow (pure `chooseAgent` selection; only executes after the user approves; high-risk → always confirmed, never persistently approvable). `executeMissionStep` maps each tool to a real action (app launch, reveal, file/note search, Control Center nav, dispatch, restart, Quick AI sub-task) with **no generic run-command branch**; all native calls injected → headless-tested. **Orbit Agent view** (command "Orbit Agent" + NL: "agent: …", "have an agent …", "ask orbit to …", "… for me"): provider/Relay status banners, plan preview with per-step risk/rationale, per-step **confirmation gating** (Approve/Skip), sequential execution with **Stop**, bounded per-step results, "Open result" navigation. **Honest gaps:** multi-step AI planning needs a real provider (offline Mock can't plan — the view says so); **model routing preview** is owned by the AgentOS Model Gateway `/route` and surfaces once its adapter exists (not duplicated); mission persistence/audit/handoffs + a unified Approval Centre + remote (Gateway/Hermes) dispatch are later. See [docs/architecture/AI_AGENT_MODE.md](docs/architecture/AI_AGENT_MODE.md) |
| Specialist agents (`@orbit/agents`) | 🟡 | **Phase 10 foundation (5 tests).** A catalogue of the specialist agents Orbit surfaces (Codex/Claude Code/Antigravity/OpenClaw) with their genuine strengths + source (relay/gateway), and a pure task→specialist **recommendation** (`recommendAgent`) shown as a *hint* in a mission's agent-dispatch step ("Suggested: … — Relay/Hermes make the final choice"). **Scope boundary honoured:** this is an agent *hint*, not orchestration or model selection — Hermes still selects/sequences agents and the Model Intelligence Gateway still selects models; Orbit does not duplicate either. Named user preferences (codex/claude/…) already flow through `chooseAgent` to the real Relay launch. Live agent *connection* (beyond Relay's local list) + per-agent capability negotiation arrive with the Gateway. |
| Automations (`@orbit/automations`) | 🟡 | **Phase 11 foundation (7 tests) — the "saved automation" mode.** An automation is a named, ordered list of **whitelisted tool steps** that `buildAutomationPlan` compiles into a `MissionPlan` and runs through the *same* Orbit Agent executor — so it inherits every mission safety property (existing-tools-only, per-step confirmation gating). 5 curated starters built from real capabilities (Review Pending Approvals, Triage Failed Sessions, Catch Up on Activity, Summarise Clipboard, Restart Relay); a Root Search provider opens each in Orbit Agent with a prebuilt plan (`auto:<id>`), landing on the preview. **Honest scope:** only the **manual** trigger is active — scheduled / app-opened / file-changed / session-completed triggers are enumerated but need native wiring (scheduler + watchers); a user-editable automation builder + import/export are later. |
| Profile & Memory (`@orbit/profile`) | 🟡 | **Phase 12 foundation (11 tests) — profile and memory kept strictly separate (§18).** **Profile** = user-edited facts (name, role, expertise, writing style, answer format, preferred agents, timezone); **Memory** = optional, summarised records, each individually inspectable + deletable, with a **global on/off** + **private mode** (off by default — no hidden memory). Pure `buildContext` returns exactly which items would be sent, honouring the memory switch and **excluding sensitive-looking items when they would leave the device** (`looksSensitive` flags keys/secrets/cards); context is rendered as data, not instructions (injection defence). Wired into **Quick AI**: a removable **Personal** context chip + a **"Why this context?"** inspector showing each included profile/memory item, its kind, sensitivity, and on-device vs leaves-device — true memory transparency (§18.3). Persisted in the settings KV (no migration); a **Settings → Profile & Memory** section edits facts, toggles/deletes/clears memories, and controls the global + private switches. **Also (bridge follow-up):** Settings → AI now offers **Local Ollama** as a real, selectable provider. **Honest gaps:** automatic memory *creation* from interactions, vault-backed durable memory (via the Gateway), and a profile/memory influence inspector inside Chat are later. |
| AI Chat (`@orbit/chat` + `orbit-core::chat`) | 🟡 | **Phase 5 — persistent multi-turn Chat, built end-to-end (13 tests: 7 pure + 6 Rust).** **Real SQLite persistence** (migration 0009: `chats` + `chat_messages`; `orbit-core::chat` CRUD with monotonic per-chat `seq`, branch + delete-from-seq, 6 tests) behind typed IPC commands (`chat_list/create/rename/set_pinned/delete/add_message/messages/delete_from/branch`). Pure `@orbit/chat`: `buildChatMessages` (system prompt + most-recent turns within a char budget), `deriveTitle`, a minimal/safe Markdown block parser (fenced code + text; tolerates a mid-stream unterminated fence) and a Markdown export. **`ChatView`** (command "AI Chat"): conversation list (new/select/search/pin/delete), streamed responses against the **live provider** (real Ollama), **Stop**, **Regenerate** (drops the tail + re-streams), per-message **Copy**, **Export** transcript, editable title, on-device/leaves-device badge, honest "no provider" state. Messages framed as data, not instructions. **Tool-use now live (session 17) — "control AgentOS with AI":** a **Tools** toggle lets the model *call* the connected MCP tools (AgentOS + any MCP server) via function-calling. The provider contract gained `tools`/`toolCalls` (Ollama + Mock adapters implement it; OpenAI-compat/Anthropic are follow-ups), and a pure, unit-tested `runToolLoop` (5 tests) offers the registry tools, parses tool calls, gates **consequential calls through confirmation** (medium-risk → "allow for this session"; high/critical → once-only, never persistent), executes through the `McpClient`, feeds bounded results back, and renders **tool-call cards** (tool, server, risk, args, status, result, duration). Chat **auto-selects a valid, tool-capable model** (`modelPick`, the configured default may be an unpulled tag; the original llama3/gemma/phi don't support tools); a non-tool model surfaces Ollama's "does not support tools" error with guidance. **Verified end-to-end against the real stack** — llama3.1:8b called AgentOS `get_battle_plan`, got the real vault data back, and grounded its answer. **Honest gaps:** tool-use is wired into Chat (not Quick AI); tool-call cards are live-only (not yet persisted across reload); attachments/citations are later; answers are only as real as the configured provider/model. |
| MCP client + Tool Registry (`@orbit/tool-registry`) | 🟡 | **Phase 8 foundation, pure + unit-tested (39 tests).** Unified `ToolRecord` vocabulary across sources (native/relay/agentos/extension/mcp/ai-provider); **risk + approval policy derived from declared side effects** — anything that writes/deletes/sends/runs/pushes/publishes/deploys/spends is forced through confirmation, and high/critical-risk tools are restricted to **once-only** approval (never persistent, per §21). **MCP client** speaks JSON-RPC 2.0 over an injected `McpTransport` (initialise, tools/resources/prompts discovery, `tools/call` with **bounded** output and failure isolation — a misbehaving server yields a typed `McpError`, never an exception). An offline `MockMcpTransport` exercises the whole path; the **native catalogue** (`nativeToolRecords`: open-app, open-folder, control-center nav, find files/notes, **dispatch agent via Relay**, restart Relay, Quick AI) registers Orbit's own safe capabilities so native + MCP tools are uniform. Renderer: `buildToolRegistry()` (native + a clearly-labelled **in-process demo MCP server**) and an **MCP & Tools** view (registry by source, risk/side-effect/approval badges, schema inspector, gated test-call). Untrusted MCP annotations only ever *raise* caution (an unannotated tool defaults to a gated write). **Status (native bridge landed, session 12): real HTTP MCP servers now connect** — `NativeHttpMcpTransport` POSTs JSON-RPC through the native HTTP client (parses plain-JSON *or* SSE `data:` frames), server configs persist in the settings KV (`mcp.servers`, http(s)-only, validated), the **MCP & Tools** view has an add/enable/remove panel, and `buildToolRegistry` connects enabled servers + registers their tools (an unreachable server is skipped, never breaks the registry). **Stdio MCP now live (session 17):** a native process-host (`mcp_stdio.rs`: `mcp_stdio_open/request/logs/close`, newline-delimited JSON-RPC framed by id, stderr drained to a bounded ring; round-trip Rust test against real `node`) backs `NativeStdioMcpTransport`, so long-lived stdio MCP servers connect. The MCP & Tools view has a **one-click "Connect AgentOS"** that runs `node <dir>/server/mcp/server.js` and registers its 8 Second-Brain tools (verified end-to-end against the real AgentOS server). Test-calls now work against any connected server (args via a single box: plain text → first required string field, or `{json}`). **Remaining:** credential/secure-storage for authenticated MCP servers. See [docs/architecture/MCP_TOOL_REGISTRY.md](docs/architecture/MCP_TOOL_REGISTRY.md) |
| Cloud sync / account / teams | ⬜ | — |
| Browser extension | ⬜ | — |

## Security

| Item | Status | Notes |
| --- | --- | --- |
| Deeplink validation (allowlisted routes) | ✅ | `@orbit/validation`, tested |
| Path-traversal / containment guards | ✅ | tested |
| Shell-argument & external-URL safety | ✅ | tested |
| No `eval` in calculator | ✅ | recursive-descent parser |
| Confirmation/preview for consequential actions | 🟡 | Pure policy (`confirm.ts`: `needsConfirmation`/`describeConfirmation`, tested) + a modal `ConfirmDialog` (Enter confirms, Esc/scrim cancels) wired into the launcher's single action choke-point (`App.runItem`). Any action marked `dangerous` shows a preview the user must approve before it runs; the natural-language "Restart Relay" intent is the first to use it. Broader coverage (paste/replace, file deletion, Git push, deployment, spend) and a unified Approval Centre are later slices |
| Manifest hardening (lengths, identifiers, no traversal) | ✅ | tested |
| Extension process isolation + permission broker | 🟡 | extensions run as isolated child processes; effects/item-actions brokered against declared permissions; namespaced storage; crash-loop breaker (`orbit-extensions`, tested). **Not** OS-sandboxed yet (child has Node privileges) — see EXTENSION_RUNTIME.md |
| Secret vault (OS secure storage) | 🟡 | API keys stored in **OS secure storage** via the `keyring` crate (Windows Credential Manager / macOS Keychain / Linux Secret Service) — `secret_set/get/delete/has` IPC, `service = "orbit"`. The cloud AI key uses it (`ai.cloud.apikey`); never written to SQLite/JSON/logs/git. Broader credential management (per-provider keys, MCP server auth) builds on this |
| OS sandboxing of extensions | ⬜ | designed in SECURITY_MODEL |
