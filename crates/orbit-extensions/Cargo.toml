[package]
name = "orbit-extensions"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Extension manifest, RPC protocol, permission broker and crash tracking for Orbit."

[lib]
name = "orbit_extensions"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
