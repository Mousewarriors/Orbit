[package]
name = "orbit-files"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Filesystem walker + indexing rules for Orbit local file search."

[lib]
name = "orbit_files"
path = "src/lib.rs"

# Pure indexing rules (filtering, extension/hidden detection) are platform-free
# and unit-tested; the walker uses only std::fs so it works on all platforms.
